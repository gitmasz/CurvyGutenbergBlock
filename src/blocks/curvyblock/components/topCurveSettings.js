import {
	HorizontalRule
} from "@wordpress/components";

export const TopCurveSettings = (props) => {
  return (
    <>
      <HorizontalRule />
      Enabled top curve settings content.
    </>
  );
}